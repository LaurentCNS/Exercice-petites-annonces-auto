{% extends 'back/base.html.twig' %}

{% block title %}
    Gestion Marques
{% endblock %}

{% block body %}
    <h1>Marque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brand.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brand.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-warning" href="{{ path('app_brand_index') }}">Retour</a>

    <a class="btn btn-secondary"  href="{{ path('app_brand_edit', {'id': brand.id}) }}">Modifier</a>

    {{ include('back/brand/_delete_form.html.twig') }}
{% endblock %}
