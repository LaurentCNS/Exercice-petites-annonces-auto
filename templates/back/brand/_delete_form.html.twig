<form method="post" action="{{ path('app_brand_delete', {'id': brand.id}) }}" onsubmit="return confirm('Etes vous sur de supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ brand.id) }}">
    <button class="mt-5 btn btn-danger">Supprimer</button>
</form>
