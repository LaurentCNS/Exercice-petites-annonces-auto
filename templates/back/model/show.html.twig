{% extends 'back/base.html.twig' %}

{% block title %}
    Gestion des modèles
{% endblock %}

{% block body %}
    <h1>Model</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ model.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ model.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ model.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-warning" href="{{ path('app_model_index') }}">Retour</a>

    <a class="btn btn-secondary" href="{{ path('app_model_edit', {'id': model.id}) }}">Modifier</a>

    {{ include('back/model/_delete_form.html.twig') }}
{% endblock %}
